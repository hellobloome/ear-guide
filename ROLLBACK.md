# Rollback

If a production deployment must be reversed, redeploy `Bloome_Guide_Package_32_5_4_Direct_3D_Attribute_Fix.zip`, retained alongside the 1.0 release in the project outputs folder.

Do not mix files from the production and rollback archives. Replace the deployed package as a complete set so cache-versioned assets remain consistent.
